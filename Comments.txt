1) Папку api стоит переложить в src.
2) Удалить неиспользуемую картинку с говядиной ('beef.jpg').
2) Именование компнонентов, желательно, делать минимум из двух слов PascalCase, чтобы отличать нативный html-элементы (теги) от кастомных (компоненты).
3) В компоненте Header перенести часть 'template' наверх, чтобы все компоненты структурно выглядели идентично. 
4) В компоненте Header при валидации props c множеством параметров (type, required и т.д.) необходимо использовать объектную запись (cart: {...}).
5) Использовать :key совместно с v-for для лучшей работы алгоритмов vue, при чем значение key должно быть неизменяемым, например, id.
6) Вынести карточку продукта из компонента List в отдельный компонент.
7) В карточке продукта для картинки использовать динамический alt. Если картинка отстуствует, можно без усилий понять какая именно.
8) Нельзя добавить продукт с количеством равным 0, также если корзина не содержит определенного продукта, нельзя применить отрицательное значение.
9) В public/index.html установить атрибут lang="ru", т.к. сайт на русском языке (повышается доступность).
10) Поменял бы заголовки для header h1 и тег header вместо div, для карточек h2. (Так следует из спецификации html) 
11) Перенести cartPrice в store и сделать вычисляемы свойством, а не наблюдаемым (getters).
12) Логику работы с корзиной и продуктами вынести в отдельные модули vuex.
13) При расчете ширины карточки window.innerWidth возвращает число, а сравнение происходит со строкой. Использовать на window событие resize вместо computed, 
т.к. он кэширует значение и приходится обновлять страницу для корректного отображения.
14) Переименовать классы по классификации БЭМ  block-name__elem-name.
15) В компоненте List у метода startPricesMonitoring() изменить интервал на 2000 вместо 1000, чтобы соответвовало ТЗ.
16) Вызывать функцию слежения startPricesMonitoring() в хуке mounted вместо created, не думаю, что критично, но рекомендуется загружать данные в mounted.
17) Для инпута с вводом количества товаров лучше использовать v-model, $refs используется, когда нужно управлять элементом напрямую.
18) В данном случае cart.reverse работает не так, как ожидается, поэтому добавлять в корзину самый простой способ с помощью unhift, чтобы не делать манипуляций с разворотом массива.
19) В компоненте List для блока <style> добавить scoped, чтобы стили касались только данного компонента.
20) Если продукт уже находится в корзине нужно перезаписать только его количество (amount), если количество <= 0, удалять этот продукт.